import 'package:flutter/material.dart';

// Define the overall app theme using ThemeData
final ThemeData appTheme = ThemeData(
  primarySwatch: Colors.blue, // Primary color swatch for the app
  textTheme: const TextTheme(
    titleLarge: TextStyle(fontSize: 24.0, fontWeight: FontWeight.bold), // Custom text style for large titles
    bodyLarge: TextStyle(fontSize: 18.0), // Custom text style for large body text
  ),
);
